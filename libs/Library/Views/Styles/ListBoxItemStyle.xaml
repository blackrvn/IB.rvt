<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ListBoxItem" x:Key="GreyListBoxItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="ItemBorder"
                    Background="Transparent"
                    CornerRadius="5"
                    Margin="{TemplateBinding Margin}">
                        <ContentPresenter HorizontalAlignment="Left" 
                                VerticalAlignment="Center"
                                Margin="10,5"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Default state -->
                        <Trigger Property="IsSelected" Value="False">
                            <Setter TargetName="ItemBorder" Property="Background" Value="Transparent"/>
                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                        <!-- MouseOver but not selected -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ItemBorder" Property="Background" Value="WhiteSmoke"/>
                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="WhiteSmoke"/>
                        </MultiTrigger>
                        <!-- Selected (whether MouseOver or not) -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="LightGray"/>
                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="LightGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ListViewItem" BasedOn="{StaticResource ResourceKey=GreyListBoxItem}" x:Key="GreyListViewItem"/>
    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ResourceKey=GreyListBoxItem}" x:Key="GreyComboBoxItem"/>
</ResourceDictionary>