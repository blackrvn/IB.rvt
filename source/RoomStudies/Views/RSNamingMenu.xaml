<UserControl x:Class="RoomStudies.Views.RSNamingMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoomStudies.Views"
             xmlns:viewModel="clr-namespace:RoomStudies.ViewModels"
             xmlns:userControls="clr-namespace:Library.Views.UserControls;assembly=Library"
             d:DataContext="{d:DesignInstance viewModel:RSNamingMenuViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Left Panel: Available Placeholders -->
        <Border Grid.Column="0" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="5">
            <ListView ItemsSource="{Binding GroupedPlaceholders}">
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="2"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ParameterName}" 
                       Margin="3" Padding="2" Background="LightGray" 
                       MouseMove="Placeholder_MouseMove"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Right Panel: Blueprint Builder Area -->
        <Border Grid.Column="2" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="5"
            AllowDrop="True" DragOver="BlueprintBuilder_DragOver" Drop="BlueprintBuilder_Drop">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding BlueprintElements}" Name="BlueprintItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="DarkGray" BorderThickness="1" Margin="2" Padding="3">
                                <TextBlock Text="{Binding}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Button to insert static text -->
        <Button Grid.Row="1" Grid.Column="2" Content="Insert Static Text"
            HorizontalAlignment="Right" Margin="5"
            Click="InsertStaticText_Click"/>
    </Grid>
</UserControl>
